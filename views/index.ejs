<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Page Title</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script type="text/javascript">
    $(function(){
        $('#button').on('click', function() {
          $.get("/temperature", function(data, status, xhr) {
                if (status == "success") {
                    $("#id_title").html(data);
                }
            });
        });
    });

    setInterval(() => {
        $.get("/temperature", function(data, status, xhr) {
                    if (status == "success") {
                        $("#id_title").html(data);
                    }
                });
    }, 3000);

    </script>
</head>
<body>
    <h1>Landing page</h1>
    <!-- コードから渡されたデータを表示-->
    <p id="id_title"><%= title %></p>

    <button id="button">ボタン</button>

</body>
</html>